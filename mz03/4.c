struct Elem
{
    struct Elem *next, *prev;
    int count;
    char *str;
};

struct Elem* process(struct Elem* head) {
    Elem* now = head;
    while (now != NULL) {
        
    }
}

int main() {

}